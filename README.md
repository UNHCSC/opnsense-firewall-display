# opnsense-firewall-display

A program to consume log events from the OPNSense firewall over remote syslog and summarize, infer, and display metrics about firewall logs.

```bash
bash -i <(curl -sSL https://raw.githubusercontent.com/UNHCSC/opnsense-firewall-display/main/scripts/install.sh)
```